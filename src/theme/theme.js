import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#4326FB",
    },
    secondary: {
      main: "#BEB3FF",
    },
    danger: {
      main: "#F84335",
    },
    background: {
      main: "#DCDCDF",
      secondary: "#A0ABAC",
    },
  },
});
